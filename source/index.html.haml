.slides-wrapper
  %section#presentation
    %article
      %h1 Simple presentation engine
      .pointer Click to continue -->

    %article
      %h1
        You can use
        %kbd Left
        or
        %kbd Right
        arrow keys

    %article
      %h1
        And others...
      %p
        <kbd>PgUp</kbd>,
        <kbd>PgDown</kbd>,
        <kbd>Home</kbd>,
        <kbd>End</kbd>,
        <kbd>Space</kbd>

    %article
      %h1 This engine supports
      %ul
        %li
          Different tags
          %ul
            %li Lists
            %li Code
            %li Blockqoute
            %li Images
            %li Tables
            %li Links
        %li Navigation (with mouse, keyboard and location hash)
        %li Automatic font-size tuning

    %article
      %h1 It doesn't supports (yet)
      %ol
        %li Javascriptlessness :)
        %li Code highlighting
        %li Markdown
        %li Transform animation effects
        %li
          \...And other things
          = link_to 'impress.js', 'http://bartaz.github.com/impress.js', target: '_blank'
          does

    %article
      %h1 Usage
      %p
        Put each page of your presentation into an
        %code
          &lt;article&gt;
        tag.
      %p Wrap pages with some element.
      %pre
        %code
          = preserve do
            :escaped
              <section id='presentation'>

                <article>
                  <h1>Simple presentation engine</h1>
                  <p>Click to continue</p>
                </article>

              </section>

    %article
      %h1 Usage
      %p Then include jquery.js, presentation.js and this code:
      %pre
        %code
          :preserve
            $(function() {
              $('#presentation').present({
                prevBtn: '.btn.prev',
                nextBtn: '.btn.next',
                fadeDelay: 300 // default: 0
              });
            });

    %article
      %h1 Image
      = image_tag 'linus.jpg'

    %article
      %blockquote
        Abstruse quote
      %table
        %tr
          %td Here
          %td should
          %td be
        %tr
          %td Some
          %td reasonable
          %td table
        %tr
          %td but
          %td ...

    %article
      %h1 That's it
